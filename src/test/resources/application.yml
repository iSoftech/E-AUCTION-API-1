server:
  port: 5050
  servlet-path: /

spring:
  application:
    name: e-auction-seller-service
  data:
    mongodb:
      database: test_db_seller
      uri: mongodb+srv://mongo-user:m0ngo-P%40%24%24@cluster0.i5tgr.mongodb.net/test_db_seller?retryWrites=true&w=majority

info:
  app:
    name: ${spring.application.name}
  build:
    version: ${project.version}
